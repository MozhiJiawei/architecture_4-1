# architecture_4-1

This repository hosts a minimal skill for documenting and working with the 4+1 architectural view model.

Current scope includes:

- logic, runtime, and use-case intermediate-model plus rendering workflow
- development/code-view intermediate-model schema, rendering, and guidance for subagent handoff
- reference-driven review against the sample diagrams in `ref/`
- development-view fallback pruning guidance that preserves core business relationships while dropping noisy support/utils nodes first
